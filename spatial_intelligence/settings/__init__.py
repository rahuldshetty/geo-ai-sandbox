"""Application settings: environment/config resolution and persisted preferences.

Ported from ``geoai/config.py`` and ``geoai/settings.py``; the data-root layout,
env-var contract, and ``settings.json`` keys are unchanged so an existing
checkout keeps working.
"""
